var height = 5;
for (var row = 0; row < height; row++) {
    var string = '';
    for (var col = 0; col <= row; col++) {
        string = string + "*";
    }
    console.log(sjgtring);
    j
}